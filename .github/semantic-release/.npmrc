# Checked natively before install, so it still applies with lifecycle scripts
# blocked. Enforces the engines block in package.json, which pins the release
# job to the package manager version this lockfile was produced with.
engine-strict=true
